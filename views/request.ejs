<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Requests</title>
    <style>
        #request_container {
            display: flex;
            flex-direction: row;
            flex-wrap: wrap;
            text-align: center;
            justify-content: center;
            align-items: center;
            width: 100%;
        }

        #request_box {
            border: 1px solid black;
            box-shadow: 0 0 10px rgb(12, 215, 29);
            font-family: 'Roboto Slab', serif;
            text-align: center;
            justify-content: center;
            align-items: center;
            margin: 15px;
            width: 28%;
            padding-bottom: 10px;
        }
    </style>
</head>

<body>
    <div id="request_container">
        <% for(var i=0;i<data.length;i++) { %>
            <div id="request_box">
                <p>Sender Name:
                    <%= data[i].sender_name %>
                </p>
                <p>Sender Phone:
                    <%= data[i].sender_phone %>
                </p>
                <p>Request for :
                    <%= data[i].item %>
                </p>
                <p>Request Time:
                    <%= data[i].date %>/
                        <%=data[i].month%>/
                            <%=data[i].year %>
                </p>
            </div>
            <% } %>
    </div>
</body>

</html>
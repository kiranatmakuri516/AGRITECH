<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products</title>
    <style>
        #product_container {
            display: flex;
            flex-direction: row;
            flex-wrap: wrap;
            text-align: center;
            justify-content: center;
            align-items: center;
            width: 100%;
        }
        
        #product_box {
            border: 1px solid black;
            box-shadow: 0 0 10px rgb(12, 215, 29);
            font-family: 'Roboto Slab', serif;
            text-align: center;
            justify-content: center;
            align-items: center;
            margin: 15px;
            width: 28%;
            padding-bottom: 10px;
        }
        
        #pro-img {
            width: 20vw;
            height: 25vh;
            margin-top: 15px;
            box-shadow: 0 0 10px rgb(12, 215, 29);
        }
        
        p {
            font-size: 20px;
        }
        
        #crop-name {
            font-weight: 600;
        }
        
        #f1 {
            float: right;
            margin-right: 5px;
        }
        
        #search {
            margin-top: 10px;
        }
    </style>

</head>

<body>

    <div id="search">
        <form action="/search_admincrop" method="POST">
            <input type="submit" value="Search" id="f1">
            <input type="text" name="crop_name" id="f1">
            <br><br>
        </form>
    </div>
    <div id="product_container">
        <% var i=0 %>
            <% for(i=0;i<data.length;i++) { %>
                <div id="product_box">
                    <img src="<%= data[i].image %>" id="pro-img">
                    <p id="crop-name">
                        <%= data[i].name %>
                    </p>
                    <form action="/updateproduct/<%=data[i]._id %>" method="post">
                        <p>Cost:<input type="text" value="<%=data[i].price %>" name="price"></p>
                        <input type="submit" value="Update">
                    </form>
                </div>

                <% } %>
    </div>
    </div>
    </div>
</body>

</html>